sudo: required 
language: node_js
node_js:
    - "stable"
    
services: 
    - docker
before_install: 
    - docker build -t cathyyfyang/sample-docker-react -f Dockerfile.dev .
script:     
    - docker run -e CI=true cathyyfyang/sample-docker-react npm run test -- --coverage --watchAll=false
